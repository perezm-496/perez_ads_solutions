def tower(n, source, target, aux):
    """
    Describes the movements necessary to move n disks from source to target using aux,
    according to the Hanoi towers rules.
    """
    if n == 1:
        print(f"Move disk {1} from {source} to {target}")
    else:
        tower(n-1, source, auxiiary, target)
        print(f"Move 
